const fs = require("fs");
function esc(s="") { return String(s).replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])); }
module.exports = function htmlReporter(report, file) {
  const cards = report.checks.map(c => `<article class="card"><div class="head"><h2>${esc(c.name)}</h2><span class="${c.status.toLowerCase()}">${c.status}</span></div><div class="score">${c.score}/100</div>${c.issues.length ? `<ul>${c.issues.map(i => `<li class="${i.level}"><b>${esc(i.code)}</b> — ${esc(i.message)} ${i.file ? `<code>${esc(i.file)}</code>` : ""}</li>`).join("")}</ul>` : `<p class="ok">Sin problemas.</p>`}</article>`).join("");
  const html = `<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>WinHacks QA Report</title><style>body{margin:0;background:#071019;color:#eef6ff;font-family:Inter,system-ui;padding:32px}.wrap{max-width:1100px;margin:auto}.hero{padding:28px;border:1px solid #28445d;border-radius:18px;background:#0e1d2b}.big{font-size:54px;font-weight:900;color:#35e58a}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:14px;margin-top:18px}.card{padding:20px;border:1px solid #28445d;border-radius:14px;background:#0e1d2b}.head{display:flex;justify-content:space-between;gap:15px}.pass{color:#35e58a}.fail{color:#ff667a}.score{font-size:28px;font-weight:900;margin:10px 0}li{margin:8px 0;color:#b8cad9}.error{color:#ff9aaa}.warning{color:#ffd783}code{display:block;margin-top:5px;color:#9acaff}.ok{color:#35e58a}</style></head><body><div class="wrap"><section class="hero"><h1>WinHacks QA Engine</h1><div class="big">${report.score}/100</div><p>Errores: ${report.summary.errors} · Warnings: ${report.summary.warnings} · Checks: ${report.checks.length}</p><p>${esc(report.generatedAt)}</p></section><div class="grid">${cards}</div></div></body></html>`;
  fs.writeFileSync(file, html);
};
